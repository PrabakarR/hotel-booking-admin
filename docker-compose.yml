services:
  api:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    environment:
      PORT: 3001
      FRONTEND_URL: http://localhost:3000
    # Database is Supabase PostgreSQL — no local Postgres container.
